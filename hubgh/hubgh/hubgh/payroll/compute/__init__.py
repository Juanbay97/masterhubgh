"""Módulos de cálculo del pipeline payroll.

Cada módulo expone funciones puras que reciben una `Payroll Novedad` (en
memoria) + contexto del empleado/contrato + parámetros globales, y devuelven
`computed_amount` y/o `computed_quantity`.

Implementación detallada en Fase D.
"""
