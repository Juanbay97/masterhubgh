services:
  mariadb:
    image: mariadb:10.6
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-frappe}
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p${MARIADB_ROOT_PASSWORD:-frappe}"]
      interval: 10s
      timeout: 5s
      retries: 15

  redis-cache:
    image: redis:7-alpine

  redis-queue:
    image: redis:7-alpine

  backend:
    image: frappe/bench:latest
    env_file:
      - ../.env
    environment:
      SHELL: /bin/bash
      HUBGH_RUNTIME_MODE: production
      FRAPPE_GUNICORN_WORKERS: ${FRAPPE_GUNICORN_WORKERS:-2}
      FRAPPE_GUNICORN_MAX_REQUESTS: ${FRAPPE_GUNICORN_MAX_REQUESTS:-5000}
      FRAPPE_GUNICORN_MAX_REQUESTS_JITTER: ${FRAPPE_GUNICORN_MAX_REQUESTS_JITTER:-500}
      FRAPPE_HTTP_TIMEOUT: ${FRAPPE_HTTP_TIMEOUT:-120}
    volumes:
      - bench_data:/home/frappe/frappe-bench
      - ../hubgh:/home/frappe/frappe-bench/apps/hubgh:ro
      - sites_data:/home/frappe/frappe-bench/sites
      - ./runtime-common.sh:/runtime-common.sh:ro
      - ./entrypoint.prod.sh:/entrypoint.prod.sh:ro
      - ./create-site.sh:/create-site.sh:ro
    depends_on:
      mariadb:
        condition: service_healthy
      redis-cache:
        condition: service_started
      redis-queue:
        condition: service_started
    expose:
      - "8000"
      - "9000"
    command: ["bash", "/entrypoint.prod.sh"]

  caddy:
    image: caddy:2.8-alpine
    depends_on:
      backend:
        condition: service_started
    env_file:
      - ../.env
    environment:
      PUBLIC_DOMAIN: ${PUBLIC_DOMAIN}
      ACME_EMAIL: ${ACME_EMAIL}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - bench_data:/home/frappe/frappe-bench:ro
      - ../hubgh:/home/frappe/frappe-bench/apps/hubgh:ro
      - sites_data:/home/frappe/frappe-bench/sites:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  mariadb_data:
  bench_data:
  sites_data:
  caddy_data:
  caddy_config:
